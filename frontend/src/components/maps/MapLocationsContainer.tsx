import { MapLocationsContainerProps } from "@/src/types/map"

export default function MapLocationsContainer({children}: MapLocationsContainerProps)
{
    return (
        <div>
            {children}
        </div>
    )   
}
