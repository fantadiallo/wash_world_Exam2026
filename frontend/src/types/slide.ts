export type SlideProps =
{
    id?: string,
    slideTopicText: string,
    slideHeaderText: string,
    slideSubheaderText: string,
    imageSrc: string,
    imageAlt: string,
    children?: React.ReactNode,
}